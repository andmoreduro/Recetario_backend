{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma": "dotenv -e .env -- prisma",
    "migrate": "npm run prisma -- migrate dev",
    "postinstall": "npm run prisma -- generate",
    "seed": "dotenv -e .env -- node prisma/seed.js",
    "db:reset": "npm run prisma -- migrate reset --force",
    "db:setup": "npm run prisma -- migrate deploy && npm run seed",
    "build": "cd .. && npm install && npm run build && rm -rf backend/dist && mv dist backend/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "argon2": "^0.44.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.19.0"
  }
}
